@{
  ViewData["Title"] = "ChannelAdvisor";
}

<form asp-action="ProductsByLastSoldDate" method="post">
  <div class="form-group row">
    <label for="lastSoldDate" class="col-sm-2 col-form-label">Date</label>
    <div class="col-sm-10">
      <input type="date" class="form-control-plaintext" id="lastSoldDate" name="lastSoldDate">
    </div>
  </div>
  <button type="submit" class="btn btn-primary">Generate</button>
</form>